(function (){
    $.global.processWhen = {};

    function main(data_block){
        /*
        Test for processing "when" in data_block.
        Return data_block
        */

        if (! data_block.when){
            return;
        }

        LOG("processWhen.jsx");
        LOG(data_block.when);

        return data_block;
    }
    $.global.processWhen.main = main;
})()