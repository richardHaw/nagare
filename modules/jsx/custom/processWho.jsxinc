(function (){
    $.global.processWho = {};

    function main(data_block){
        /*
        Test for processing "who" in data_block.
        Return data_block
        */

        if (! data_block.who){
            return;
        }

        LOG("processWho.jsx");
        LOG(data_block.who);

        return data_block;
    }
    $.global.processWho.main = main;
})()